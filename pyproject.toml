[project]
name = "gpu-search-experiments"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["mlx==0.31.1; sys_platform == 'darwin' and platform_machine == 'arm64'", "numpy==2.4.3", "pyyaml==6.0.3"]
